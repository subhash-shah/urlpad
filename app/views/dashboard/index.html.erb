<h2>Urlpad Home</h2>
<table>
  <tr>
    <th>Tags</th>
  </tr>
  <% @tags.each do |tag| %>
    <tr>
      <td><%= link_to tag.name, links_search_path(q: tag.name) %></td>
    </tr>
  <% end %>
</table>
<br />
<%= link_to "Add New Tag", new_tag_path %>